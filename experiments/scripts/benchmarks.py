CHART_SIZE=26
CLOSURE_SIZE=133
LANG_SIZE=65
MATH_SIZE=106
MOCKITO_SIZE=38
TIME_SIZE=27

CLI_SIZE=40
CODEC_SIZE=18
COLLECTIONS_SIZE=28
COMPRESS_SIZE=47
CSV_SIZE=16
GSON_SIZE=18
JACKSON_CORE_SIZE=26
JACKSON_DATABIND_SIZE=112
JACKSON_XML_SIZE=6
JSOUP_SIZE=93
JXPATH_SIZE=22
CLOSURE_NEW=[i for i in range(133,175)]

CHART_LIST=[f'Chart_{i}' for i in range(1,CHART_SIZE+1)]
CLOSURE_LIST=[f'Closure_{i}' for i in range(1,CLOSURE_SIZE+1)]
LANG_LIST=[f'Lang_{i}' for i in range(1,LANG_SIZE+1)]
MATH_LIST=[f'Math_{i}' for i in range(1,MATH_SIZE+1)]
MOCKITO_LIST=[f'Mockito_{i}' for i in range(1,MOCKITO_SIZE+1)]
TIME_LIST=[f'Time_{i}' for i in range(1,TIME_SIZE+1)]

CLI_LIST=[f'Cli_{i}' for i in range(1,CLI_SIZE+1)]
CODEC_LIST=[f'Codec_{i}' for i in range(1,CODEC_SIZE+1)]
COLLECTIONS_LIST=[f'Collections_{i}' for i in range(1,COLLECTIONS_SIZE+1)]
COMPRESS_LIST=[f'Compress_{i}' for i in range(1,COMPRESS_SIZE+1)]
CSV_LIST=[f'Csv_{i}' for i in range(1,CSV_SIZE+1)]
GSON_LIST=[f'Gson_{i}' for i in range(1,GSON_SIZE+1)]
JACKSON_CORE_LIST=[f'JacksonCore_{i}' for i in range(1,JACKSON_CORE_SIZE+1)]
JACKSON_DATABIND_LIST=[f'JacksonDatabind_{i}' for i in range(1,JACKSON_DATABIND_SIZE+1)]
JACKSON_XML_LIST=[f'JacksonXml_{i}' for i in range(1,JACKSON_XML_SIZE+1)]
JSOUP_LIST=[f'Jsoup_{i}' for i in range(1,JSOUP_SIZE+1)]
JXPATH_LIST=[f'JxPath_{i}' for i in range(1,JXPATH_SIZE+1)]
CLOSURE_NEW_LIST=[f'Closure_{i}' for i in CLOSURE_NEW]

# D4J_1_2_0_LIST=CHART_LIST+CLOSURE_LIST+LANG_LIST+MATH_LIST+MOCKITO_LIST+TIME_LIST
D4J_1_2_0_LIST=CHART_LIST+CLOSURE_LIST+LANG_LIST+MATH_LIST+TIME_LIST
D4J_2_0_LIST=CLI_LIST+CODEC_LIST+COLLECTIONS_LIST+COMPRESS_LIST+CSV_LIST+GSON_LIST+JACKSON_CORE_LIST+JACKSON_DATABIND_LIST+JACKSON_XML_LIST+JSOUP_LIST+JXPATH_LIST+CLOSURE_NEW_LIST